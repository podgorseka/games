
'use client';

// This component is now effectively disabled or unused as we use local state in page.tsx
// I am keeping the file to avoid import errors if some parts still try to reference it, 
// but it will render nothing.

export function AuthButton() {
  return null;
}
